import React from "react";
import ReactDOM from "react-dom/client";
import PowerBISite from "./PowerBISite";
import "./index.css";

const root = ReactDOM.createRoot(document.getElementById("root"));
root.render(
  <React.StrictMode>
    <PowerBISite />
  </React.StrictMode>
);